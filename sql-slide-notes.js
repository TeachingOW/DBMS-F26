window.DBMS_SLIDE_NOTES = window.DBMS_SLIDE_NOTES || {};
window.DBMS_SLIDE_NOTES.sql = {
  1: ["Introduction to SQL", "SQL is the language used to define, query, and change relational data. This lecture develops a way to predict query results before running them.", "Focus on meaning first: tables, rows, columns, duplicates, and NULL determine the answer."],
  2: ["Declarative means describe the answer", "In SQL, an application states which result it needs. The DBMS may scan a table, use an index, reorder joins, or choose another equivalent plan.", "The query expresses what is correct; the optimizer decides how to obtain it."],
  3: ["Part I builds the foundation", "This part introduces SQL vocabulary, schemas, constraints, single-table queries, and joins. Later topics depend on these definitions.", "Be able to read a table and trace SELECT–FROM–WHERE before moving on."],
  4: ["Start with SQL’s data model", "This section defines tables, rows, columns, schemas, keys, and constraints. These terms describe both the stored data and the legal database states.", "A query is easier to understand when the input schema is explicit."],
  5: ["Basic SQL has three jobs", "SQL defines structures, changes stored rows, and retrieves results. The same language therefore supports both database setup and day-to-day use.", "Separate DDL, DML, and queries when explaining what a statement does."],
  6: ["SQL is high level and standardized", "SQL hides low-level storage and iteration. ANSI and ISO publish standards, while products such as PostgreSQL, MySQL, SQL Server, Oracle, and SQLite implement different dialects.", "Learn the relational core, then verify dialect-specific syntax in the target DBMS."],
  7: ["The language evolved without losing its core", "SQL began as SEQUEL in IBM’s System R work and later became an ANSI/ISO standard. New standards added features, but tables and declarative queries remain central.", "Portable SQL uses common features; advanced features may differ by vendor."],
  8: ["DDL, DML, and querying serve different purposes", "CREATE, ALTER, and DROP define structures. INSERT, UPDATE, and DELETE change rows. SELECT retrieves a result without specifying an execution algorithm.", "Classify a statement by whether it changes the schema, changes data, or returns data."],
  9: ["A table is a typed collection of rows", "Product is the relation name; PName, Price, and Manufacturer are attributes. Each displayed line is one tuple that follows the table schema.", "SQL tables behave as multisets, so repeated rows may exist."],
  10: ["A multiset keeps duplicate counts", "GizmoWorks appears in two Product rows. A multiset preserves both occurrences but does not promise a first or next row unless ORDER BY is used.", "Multiplicity is data; order is not guaranteed."],
  11: ["Every column has an atomic type", "Each Product row stores one PName, one Price, and one Manufacturer value. Standard relational columns contain single values rather than lists nested inside a cell.", "The declared type restricts which values and operations make sense."],
  12: ["A tuple is one complete table entry", "The Gizmo row combines its name, price, and manufacturer into one tuple. Every tuple must supply values compatible with the same schema.", "Row, tuple, and record are often used for the same concept."],
  13: ["Cardinality counts rows; arity counts columns", "The Product example has four tuples and three attributes, so its cardinality is 4 and its arity is 3.", "Do not confuse table size by rows with table width by columns."],
  14: ["SQL preserves duplicates unless asked not to", "A normal SELECT keeps every matching occurrence. DISTINCT and set operators such as UNION remove duplicates, while UNION ALL preserves them.", "Predict duplicate counts whenever joins or projections can repeat a value."],
  15: ["Choose a type that matches the value’s meaning", "CHAR and VARCHAR store text; integer types store whole quantities; floating types store approximate measurements. Money and dates need types that support their required operations.", "Atomic does not mean interchangeable: price, identifier, and timestamp values need different representations."],
  16: ["A schema names columns and their types", "Products(PName, Price, Category, Manufacturer) describes the allowed shape of every row. Underlining a column indicates that it participates in a key.", "A schema is a contract; the data is an instance of that contract."],
  17: ["A key is minimal and unique", "A key distinguishes every Students row. It may contain one column or several, and a table may have multiple candidate keys.", "Unique is not enough by itself—the key must not include unnecessary attributes."],
  18: ["NULL represents missing information", "Jim’s GPA is NULL because it is not known yet. NOT NULL should be used when absence would make a row invalid, such as a required student name.", "NULL is not zero or an empty string, and comparisons with it need special logic."],
  19: ["Constraints trade flexibility for protection", "A general assertion can describe a business rule, but checking arbitrary rules after every change may be expensive. Systems therefore emphasize constraints that can be enforced efficiently.", "Use database constraints for important rules, especially keys, references, and simple checks."],
  20: ["Constraints communicate meaning", "Keys identify rows, foreign keys connect tables, and other constraints limit values. The DBMS can also use this information to reject invalid updates and improve plans.", "Schema design is executable documentation of the data rules."],
  21: ["Single-table queries isolate the core pattern", "Before combining tables, practice selecting rows, choosing columns, matching text, removing duplicates, and sorting one relation.", "Master one-table reasoning before adding join multiplicity."],
  22: ["This section extends SELECT–FROM–WHERE", "The workhorse query is followed by LIKE for patterns, DISTINCT for duplicate removal, and ORDER BY for a guaranteed display order.", "Each operator changes a different property: membership, columns, multiplicity, or order."],
  23: ["Read SFW as a result specification", "FROM identifies the input relations, WHERE keeps rows satisfying a condition, and SELECT constructs the output columns.", "A useful logical reading is FROM, then WHERE, then SELECT—even though the query is written SELECT first."],
  24: ["Selection filters rows", "The condition Category = 'Gadgets' keeps Gizmo and Powergizmo and discards the other Product rows. SELECT * retains all columns of the surviving rows.", "WHERE changes cardinality, not arity."],
  25: ["Projection chooses output columns", "After filtering to Gadgets, the SELECT list returns PName, Price, and Manufacturer but omits Category.", "Projection changes the output schema and may expose duplicate rows."],
  26: ["Always name the input and output schemas", "Product has four attributes, while Answer has only the three named in SELECT. The WHERE column can be used even when it is not returned.", "Trace both the rows that survive and the columns that remain."],
  27: ["Keywords are forgiving; values are not", "SELECT, Select, and select mean the same keyword. String constants use single quotes, and their capitalization may change the value depending on collation and DBMS settings.", "Do not rely on product-specific case behavior when writing portable examples."],
  28: ["Relational algebra explains the syntax", "WHERE corresponds to selection, the SELECT list corresponds to projection, and combining relations begins with a product before applying a condition.", "Translate the question into row, column, and combination operations before typing SQL."],
  29: ["LIKE matches text patterns", "% matches any sequence of characters, while _ matches exactly one character. '%gizmo%' therefore finds names containing gizmo anywhere.", "Pattern matching is case-sensitive or insensitive according to the DBMS and collation."],
  30: ["DISTINCT changes bag output into set output", "SELECT Category returns Gadgets twice because two products have that category. SELECT DISTINCT Category returns one copy of each category.", "Use DISTINCT only when duplicate removal matches the question, not as a routine repair."],
  31: ["ORDER BY is the only ordering guarantee", "The example sorts qualifying products first by Price and then by PName to break equal-price ties. DESC reverses the direction for a named expression.", "Without ORDER BY, row order is unspecified."],
  32: ["Multi-table queries add matching and multiplicity", "This section combines relations so that a result can use related facts stored in different tables.", "Before joining, identify the relationship and the columns that establish it."],
  33: ["Three ideas organize multi-table SQL", "Foreign keys express valid references, joins combine matching rows, and join semantics explain exactly which combinations reach the output.", "Correctness begins with keys; syntax comes second."],
  34: ["A foreign key prevents orphan rows", "Enrolled.student_id refers to Students.sid. Mary may enroll because sid 123 exists; a row for student 999 must be rejected unless that student is added first.", "The child reference need not be unique, but it must match a candidate key in the parent."],
  35: ["Declare both identity and reference constraints", "PRIMARY KEY(student_id, cid) prevents the same student-course pair from appearing twice. FOREIGN KEY(student_id) REFERENCES Students(sid) requires a matching student.", "A composite key is necessary because student_id alone repeats across courses."],
  36: ["Updates must preserve referential integrity", "An invalid child insert is rejected. Deleting a referenced parent requires a policy such as RESTRICT, CASCADE, or SET NULL when the column permits NULL.", "Choose delete behavior from the business rule, not convenience."],
  37: ["Keys identify; foreign keys reference", "Product’s key distinguishes products. Manufacturer can be a foreign key that points to Company.CName, connecting each product to one company.", "The same column can be a foreign key without being unique in its own table."],
  38: ["Join the tables that hold the required facts", "Product supplies PName and Price; Company supplies Country. Manufacturer = CName connects the rows before the Japan and price filters are applied.", "Every joined table should have a clear role in answering the question."],
  39: ["A join keeps matching combinations", "Conceptually, Product and Company form possible pairs, and the join condition removes pairs whose manufacturer and company name differ.", "Missing or incorrect join conditions create extra combinations and misleading duplicates."],
  40: ["Comma joins and explicit JOIN can be equivalent", "The two queries produce the same inner-join result. Explicit JOIN ... ON keeps relationship conditions separate from filters such as Price <= 200.", "Prefer explicit JOIN syntax when it makes the intended relationship clearer."],
  41: ["Trace the join to the actual answer", "Only SingleTouch matches a Japanese manufacturer and also costs at most $200. MultiTouch is Japanese but fails the price condition.", "Check each condition independently before projecting the requested columns."],
  42: ["Duplicate column names create ambiguity", "Both Person and Company contain name and address, so an unqualified reference does not identify which table supplies the value.", "Qualify shared column names whenever more than one input can provide them."],
  43: ["Aliases make qualified references readable", "Person.name and Company.name are unambiguous; p.name and c.name express the same idea more compactly.", "Declare an alias in FROM, then use it consistently throughout the query."],
  44: ["Logical semantics enumerates combinations", "The pseudocode chooses one row from each FROM relation, applies the WHERE condition, and emits the SELECT expressions for each match.", "This model predicts the answer and duplicate count, not the fastest execution plan."],
  45: ["The R–S example produces two copies of 3", "R.A = 3 matches two S rows whose B value is 3. Projecting only R.A therefore emits 3 twice under bag semantics.", "Projection does not automatically remove duplicates."],
  46: ["Reason in three stages", "First form R × S, then keep pairs satisfying R.A = S.B, and finally return only R.A. Each stage explains a visible change in the example tables.", "Combinations → condition → projection is a dependable debugging model."],
  47: ["Semantics is not execution order", "The conceptual stages define which result is correct. A real DBMS can use a hash join, nested loops, an index, or a reordered plan.", "Do not infer performance from the logical explanation."],
  48: ["Meaning and performance require different tools", "Use the logical reading to verify rows, duplicates, and NULL behavior. Use EXPLAIN or EXPLAIN ANALYZE to inspect the physical plan.", "An optimization is valid only when it preserves the same result semantics."],
  49: ["Projection can hide the reason for duplicates", "The query asks for countries of companies making Gadgets. If one company makes several Gadget products, its country appears once for every matching product row.", "Ask whether the question wants products, companies, or unique countries."],
  50: ["The answer contains repeated USA rows", "Both Gizmo and Powergizmo match GizmoWorks, so the join emits USA twice. SELECT DISTINCT Country expresses the intended set of countries.", "Diagnose the join multiplicity before adding DISTINCT."],
  51: ["Part II adds composition and aggregation", "The lecture now moves from basic joins to set operators, nested queries, aggregate functions, grouping, NULL, and outer joins.", "Continue predicting schema, rows, multiplicity, and NULL at every step."],
  52: ["Set operators and subqueries combine results", "Set operators merge compatible query outputs. Nested queries use one query’s result as an input or test inside another query.", "Both features depend on understanding result schemas and duplicate semantics."],
  53: ["This section compares bags, sets, and nesting", "You will trace tuple counts through multiset operations and use subqueries for membership, existence, and quantified comparisons.", "Check both the values and the number of copies returned."],
  54: ["Multiset operations operate on counts", "Unlike ordinary set operations, bag operations must define how many copies of each tuple survive.", "Representing multiplicity explicitly makes UNION ALL and related operators predictable."],
  55: ["Lambda records tuple multiplicity", "λX(t) is the number of copies of tuple t in multiset X. Unlisted tuples have a count of zero.", "A set is the special case where every multiplicity is either zero or one."],
  56: ["Bag intersection uses the smaller count", "For each tuple, the intersection keeps min(λX, λY) copies. Thus a tuple appearing three times in X and twice in Y appears twice in the result.", "The rule is applied independently to every possible tuple."],
  57: ["Bag union-all adds counts", "For each tuple, the multiset union contains λX + λY copies. This differs from set union, which keeps at most one copy.", "In SQL, UNION ALL follows addition; UNION follows set behavior."],
  58: ["SQL provides explicit result-combination operators", "INTERSECT keeps common rows, UNION combines rows, and EXCEPT removes rows found in the second result. ALL requests bag behavior when the DBMS supports it.", "Both query operands must return compatible columns."],
  59: ["INTERSECT keeps rows found in both results", "Q1 and Q2 must have the same number of compatible output columns. Without ALL, duplicate copies are removed.", "The comparison is performed on the projected output tuple, not hidden source columns."],
  60: ["UNION removes duplicate result rows", "Rows from Q1 and Q2 are combined and then deduplicated. That is why repeated A values appear once.", "Use UNION when the answer is a set; otherwise consider UNION ALL."],
  61: ["UNION ALL preserves every occurrence", "The operator concatenates both query results under bag semantics, so a value appearing twice in each input appears four times in the output.", "UNION ALL is often faster because it avoids duplicate elimination."],
  62: ["EXCEPT subtracts the second result", "A row returned by Q1 is removed when the same projected row appears in Q2. EXCEPT ALL, where supported, subtracts multiplicities rather than removing every copy.", "Verify your DBMS’s support and NULL behavior."],
  63: ["Intersecting the wrong attribute answers the wrong question", "Intersecting hq_city values can combine evidence from two different companies that share a city. The requirement is about the same company operating in both locations.", "Preserve the entity identity needed to connect both conditions."],
  64: ["The joined rows reveal the identity problem", "X Co and Y Inc. both project Seattle even though each satisfies only one factory-location condition. Once company name is discarded, INTERSECT cannot distinguish them.", "Projection can remove information needed for correct matching."],
  65: ["Seattle is a false positive", "The intersection returns Seattle because it appears in both result sets, not because one company has factories in both countries.", "Set operators compare output rows literally; they do not remember row provenance."],
  66: ["Nested membership tests keep company identity", "The outer Company row is retained only if its name appears in both maker subqueries. DISTINCT then prevents repeated headquarters cities.", "Apply both conditions to the same entity before projecting a shared attribute."],
  67: ["SQL is compositional", "Every query returns a relation-like result, so that result can be consumed by another query. This lets small query blocks build more complex logic.", "Understand each subquery’s schema before using it."],
  68: ["IN compares one value with a one-column result", "The subquery returns makers of products bought by Joe Blow. The outer query returns cities of companies whose name is in that maker list.", "A subquery used with IN should return the comparison column intended by the outer predicate."],
  69: ["A flattened join may change multiplicity", "The join version can produce one city per matching purchase-product combination, while IN acts as a membership test for each company row.", "Equivalent values do not guarantee equivalent duplicate counts."],
  70: ["DISTINCT can align the two formulations", "After both queries request unique cities, the flattened join and IN version express the same set result for this example.", "State whether equivalence is under set or bag semantics."],
  71: ["ALL compares against every subquery value", "price > ALL(subquery) is true only when the product price exceeds each returned Gizmo-Works price. ANY requires at least one successful comparison.", "Check dialect support and consider what happens when the subquery is empty or contains NULL."],
  72: ["EXISTS asks whether a matching row exists", "For each Gizmo-Works product p1, the correlated subquery searches for a competing product with the same name. EXISTS needs only one match.", "The reference to p1.name makes the subquery correlated."],
  73: ["EXISTS and NOT EXISTS can express set logic", "EXISTS keeps an R row when a matching pair exists in S; NOT EXISTS keeps it when no match exists. This can replace INTERSECT and EXCEPT patterns.", "Duplicate behavior still depends on the outer relation and SELECT DISTINCT."],
  74: ["Row-value IN compares composite keys", "(A,B) IN (SELECT A,B FROM S) tests both columns together. NOT IN can behave unexpectedly when the subquery contains NULL.", "For NULL-safe anti-matching, NOT EXISTS is usually easier to reason about."],
  75: ["A correlated subquery can see an outer alias", "For each outer Movie m, the inner query finds years for the same title. The ANY comparison detects another occurrence associated with a different year.", "Track variable scope: inner aliases are local, while m comes from the outer query."],
  76: ["Correlation applies a different comparison set per row", "For each product x, the subquery selects older products from x’s manufacturer. x is returned only if its price exceeds all those prices.", "The outer row changes the inner result, which can make optimization more difficult."],
  77: ["The core SQL toolkit is now compositional", "SFW queries filter and project, set operators combine results, and subqueries express membership, existence, and quantified comparisons.", "Choose the construct that states the requirement clearly and preserves intended duplicates."],
  78: ["Rewrite according to the question", "Use a join when matching-row columns are needed, EXISTS when only existence matters, and NOT EXISTS for none or for-all logic.", "Before claiming equivalence, compare schemas, duplicate counts, and NULL behavior."],
  79: ["Aggregation changes rows into summaries", "This section introduces functions that combine many values and GROUP BY, which produces one summary row per group.", "Aggregation changes the result’s level of detail."],
  80: ["Grouping has its own logical stages", "You will use SUM, COUNT, MIN, MAX, and AVG, then trace FROM/WHERE, GROUP BY, HAVING, and SELECT.", "WHERE filters rows; HAVING filters groups."],
  81: ["Aggregate functions summarize a row set", "COUNT(*) counts rows, while AVG(price) averages non-NULL price values. SUM, MIN, and MAX similarly operate across qualifying values.", "Except COUNT(*), aggregates normally ignore NULL input values."],
  82: ["COUNT has three useful forms", "COUNT(*) counts rows, COUNT(category) counts non-NULL category values, and COUNT(DISTINCT category) counts unique non-NULL categories.", "COUNT(category) equals COUNT(*) only when category is never NULL."],
  83: ["SUM can aggregate an expression", "price × quantity computes revenue per Purchase row; SUM combines those row-level revenues. Adding WHERE product = 'bagel' limits the input first.", "Build the per-row expression before applying the aggregate."],
  84: ["The Bagel total is 50", "The two Bagel rows contribute 1×20 and 1.50×20. Their sum is 20 + 30 = 50.", "An aggregate result should be explainable from the contributing rows."],
  85: ["GROUP BY returns one total per product", "Rows after October 1, 2005 are partitioned by product. SUM(price × quantity) is then calculated separately for each product group.", "Every non-aggregate item in SELECT must identify a group."],
  86: ["Trace grouping in a fixed order", "First evaluate FROM and WHERE, then partition the surviving rows, and finally compute grouped attributes and aggregate expressions.", "This logical order explains both valid SELECT lists and final values."],
  87: ["Pass 1 determines the input rows", "FROM Purchase supplies the rows and WHERE date > '10/1/2005' removes earlier purchases before any group is formed.", "A row filtered here contributes to no aggregate."],
  88: ["Pass 2 partitions rows by product", "The surviving Bagel rows form one group and Banana rows form another. Date, price, and quantity remain available inside their groups for aggregation.", "GROUP BY defines the output grain: one row per product."],
  89: ["Pass 3 computes each group’s output", "Bagel totals 50 and Banana totals 15. The result keeps the group label and the aggregate, not every original Purchase row.", "Check the arithmetic and the number of groups."],
  90: ["GROUP BY states this summary directly", "The correlated alternative recalculates a product total for outer rows and needs DISTINCT. GROUP BY forms the product groups once and expresses the intent more clearly.", "Prefer the direct grouped formulation unless another structure is required."],
  91: ["HAVING filters after groups exist", "WHERE limits individual Purchase rows by date; HAVING SUM(quantity) > 20 removes entire product groups based on an aggregate.", "A condition on an aggregate belongs in HAVING."],
  92: ["The threshold leaves only Bagel", "Bagel’s quantity is 40, so it passes SUM(quantity) > 30. Banana totals 20 and is removed, even though its sales value was computed.", "HAVING tests the group, while SELECT reports the requested aggregate."],
  93: ["The SELECT list must match the grouping level", "S may contain GROUP BY attributes and aggregate expressions. An unrelated detail column would not have one well-defined value per group.", "If an expression is neither grouped nor aggregated, question it."],
  94: ["Grouping has four logical passes", "Evaluate FROM/WHERE, form groups, apply HAVING to each group, and compute the final SELECT expressions.", "Use this sequence to locate errors instead of rewriting the whole query at once."],
  95: ["Debug rows, groups, group filters, then output", "Write down the rows after WHERE, the grouping key, each group’s aggregate, and the rows retained by HAVING.", "This method separates row-level mistakes from group-level mistakes."],
  96: ["COUNT cannot directly contain a SELECT block", "The novice attempt treats a subquery like a scalar list inside COUNT and mixes outer and inner scopes incorrectly.", "Aggregate the Wrote rows in a valid query block, then compare the aggregate."],
  97: ["GROUP BY expresses the author requirement directly", "Join Author to Wrote, form one group per author, and retain groups with enough documents. GROUP BY already returns one row per author group.", "Use HAVING COUNT(...) for thresholds over related rows."],
  98: ["A grouped subquery can return qualifying logins", "The inner query groups Wrote by login and keeps authors with enough rows. The outer query converts those identifiers into author names.", "This two-stage version is useful when filtering entities by a grouped relationship."],
  99: ["Repeated correlated work can be expensive", "A correlated formulation may scan or aggregate Wrote for many outer authors. A grouped formulation can summarize Wrote once, though the optimizer may rewrite either query.", "Write clear SQL and confirm performance with an execution plan."],
  100: ["Advanced SQL connects logic to edge cases", "The final section applies quantifiers, three-valued NULL logic, outer joins, and semantic tracing to queries that look deceptively simple.", "Test empty inputs, missing values, and unmatched rows."],
  101: ["Some means at least one match", "The join returns a company when at least one of its products costs less than 100. DISTINCT prevents one company from appearing once per qualifying product.", "Existential questions map naturally to EXISTS or a join plus duplicate control."],
  102: ["All is best read as no counterexample", "A company qualifies only if there is no product from that company priced 100 or more. This logical rewrite turns a universal condition into an anti-match.", "Prefer NOT EXISTS when NULL could make NOT IN produce UNKNOWN."],
  103: ["NULL has several possible meanings", "A missing value may be unknown, nonexistent, or not applicable. SQL stores all of these as NULL even though their business interpretations differ.", "Decide whether a column may be NULL when designing the schema."],
  104: ["NULL propagates and creates UNKNOWN", "Arithmetic with NULL yields NULL. A comparison such as x = 'Joe' is UNKNOWN when x is NULL, producing SQL’s third truth value.", "UNKNOWN is not the same as FALSE, even though WHERE rejects both."],
  105: ["AND with UNKNOWN may reject an otherwise plausible row", "For age 20, height NULL, and weight 200, height > 6 is UNKNOWN. TRUE AND UNKNOWN AND TRUE evaluates to UNKNOWN, so WHERE omits the row.", "Trace each predicate using TRUE, FALSE, and UNKNOWN."],
  106: ["A familiar tautology fails for NULL", "age < 25 OR age >= 25 is TRUE for known ages. When age is NULL, both comparisons are UNKNOWN and UNKNOWN OR UNKNOWN remains UNKNOWN.", "A nullable column requires an explicit missing-value case."],
  107: ["Use IS NULL to include missing values", "Adding OR age IS NULL handles the third case directly. The predicates now cover known ages below 25, known ages at least 25, and missing ages.", "Never test missingness with = NULL."],
  108: ["Audit every nullable expression", "Use IS NULL, remember that WHERE retains only TRUE, treat NOT IN cautiously, and expect NULL padding from unmatched outer-join rows.", "Write the intended three-valued logic explicitly."],
  109: ["Both forms are inner joins", "JOIN ... ON and comma plus WHERE return only Product–Purchase pairs whose names match. Rows without a match on either side are omitted.", "Use explicit JOIN syntax to make relationship conditions visible."],
  110: ["Inner joins can hide important absence", "A product with no Purchase row creates no matching pair and disappears. That is correct inner-join behavior but may not match a report that needs unsold products.", "Ask which side’s unmatched rows must be preserved."],
  111: ["LEFT OUTER JOIN preserves the left table", "Every Product row appears. When no Purchase matches, the Purchase columns are filled with NULL rather than dropping the product.", "Place the table that must be preserved on the left."],
  112: ["The inner-join output contains only sellers", "Gizmo matches one purchase and Camera matches two, so the result has three rows. OneClick has no match and is absent.", "One-to-many matches increase result cardinality."],
  113: ["The left join adds OneClick with NULL", "The three matching rows remain, and the unmatched OneClick row is added with a NULL store value.", "NULL in an outer-join result may mean no matching row, not unknown stored data."],
  114: ["Outer-join direction controls preservation", "LEFT preserves unmatched left rows, RIGHT preserves unmatched right rows, and FULL preserves unmatched rows from both inputs.", "Choose direction from the question’s required population."],
  115: ["The OR condition does not avoid the cross product", "FROM R, S, T still requires a triple containing one row from every table before the WHERE condition can be tested.", "Logical FROM semantics matters even when WHERE uses OR."],
  116: ["The query resembles R intersect (S union T)", "For nonempty inputs, the condition keeps R.A values matching either S.A or T.A. However, the three-way product introduces an empty-input trap.", "An algebra-looking condition does not guarantee algebraic behavior under all inputs."],
  117: ["An empty input makes the entire product empty", "If S has no rows, R × S × T has no combinations. WHERE is never evaluated, so the query returns nothing—even when R matches T.", "Test empty relations when reasoning about multi-table OR conditions."],
  118: ["Nested loops illustrate cross-product semantics", "The simplest conceptual implementation loops through R, S, and T, then applies the OR condition. This is a teaching model rather than a physical-plan claim.", "No inner-loop iteration means no candidate row."],
  119: ["The Python version exposes the empty-loop failure", "When S is empty, the loop over s executes zero times, so the loop over t and the condition are never reached.", "Use explicit UNION or EXISTS expressions when they state the intended alternatives more safely."],
  120: ["SQL separates intent from execution", "Across selection, joins, nesting, grouping, NULL, and outer joins, the programmer defines a result while the DBMS chooses a plan.", "Accurate predictions require schema, multiplicity, and missing-value reasoning."],
  121: ["Predict before running the query", "Write the output columns, expected row count, duplicate sources, and possible NULL or UNKNOWN cases. Then compare the actual result with that prediction.", "A wrong prediction is useful when you can explain exactly which assumption failed."],
  122: ["Operations determine data types", "Use DECIMAL for exact fixed-scale arithmetic, integers for counts, floating types for approximate measurements, and VARCHAR or TEXT for human-readable text.", "Choose the type that prevents the most important failure."],
  123: ["Numeric-looking identifiers are often text", "ZIP codes and student IDs need formatting rather than arithmetic. DATE stores calendar dates; TIMESTAMP stores instants; BOOLEAN represents two states unless NULL is a meaningful third state.", "Ask whether the value is calculated, formatted, or ordered in time."],
  124: ["Defend every type with an operation and a test", "DECIMAL protects exact money, VARCHAR preserves 00501, TIMESTAMP supports event ordering, and DOUBLE PRECISION accepts measurement approximation.", "A strong choice names the use case, the prevented failure, and a boundary value."],
  125: ["Design the event-registration schema", "Choose a type, precision or length, nullability, and default for each field. Add constraints that protect identifiers, nonnegative prices, and required values.", "The table definition should encode the scenario’s rules, not merely store its labels."],
  126: ["Submit evidence, not only a CREATE TABLE statement", "Predict boundary outcomes, run the DDL and inserts, query the successful data, and explain one design revision. Capture both accepted rows and rejected cases.", "A complete submission includes SQL, results, errors, and type-choice reasoning."]
};
